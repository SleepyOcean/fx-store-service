<?xml version="1.0" encoding="UTF-8" ?>
<jpa module="userJpql">
    <jpql id="findUser">
        select user_id, delivery_info, user_name from csg_user where 1=1
        #if($id)
        and user_id = :id
        #end
    </jpql>
</jpa>